# Keep the generated pins snapshot LF-only on every platform so the codegen
# `--check` byte comparison stays strict and platform-independent.
packaging/nexus-scholar/nexus_scholar_pins.json text eol=lf