name: scholar-pdf-kit CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'scholar-pdf-kit/**'
      - 'slr-search-kit/**'
      - '.github/workflows/scholar-pdf-kit-ci.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'scholar-pdf-kit/**'
      - 'slr-search-kit/**'

jobs:
  test:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4
    
    - name: Install uv
      uses: astral-sh/setup-uv@v2
      with:
        version: "latest"

    - name: Set up Python ${{ matrix.python-version }}
      run: uv python install ${{ matrix.python-version }}

    - name: Install dependencies
      working-directory: ./scholar-pdf-kit
      run: |
        uv pip install -e .[dev]

    - name: Run pytest
      working-directory: ./scholar-pdf-kit
      run: |
        uv run pytest tests/ -v
